import 'package:flutter/material.dart';

class ViewAScreen extends StatelessWidget {
  const ViewAScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CLASSES A'),
      ),
    );
  }
}
